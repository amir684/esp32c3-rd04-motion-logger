# Images

Photos, screenshots and diagrams used by the main [README](../README.md).

Suggested files (drop them in here and the README picks them up automatically):

| File | What it should show |
| --- | --- |
| `board.jpg` | The assembled ESP32-C3 SuperMini + RD-04 with the GPIO9→GPIO4 jumper |
| `wiring.png` | Wiring diagram / schematic |
| `webui.png` | Screenshot of the event log page |
| `install.jpg` | The logger installed where it is actually used |

Keep files reasonably small (< 1 MB each) so cloning stays fast.
